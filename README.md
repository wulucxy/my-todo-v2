# React + TypeScript + Vite + Convex

This template ships with Vite, Tailwind, shadcn/ui, and Convex preinstalled. Convex provisioning, deploy keys, and `VITE_CONVEX_URL` are supplied by the platform orchestrator; running outside that environment is unsupported.
