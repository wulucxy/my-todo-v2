import { defineSchema } from "convex/server";

/**
 * Minimal Convex schema so the project starts with a valid convex/ directory.
 * Add tables and indexes here as your data model evolves.
 */
const schema = defineSchema({});

export default schema;
